// The name of the arithmetic operator that gives you the remainder when one number is divided by another is called the "modulo" operator, and its symbol is "%".

// The value of num in the statement var num = 20 % 6; is 2, because it calculates the remainder of 20 divided by 6.

var largeNum = 1000 * 2000;

var result = x - y;

var myRemainder = 15 % 4; // This calculates the remainder of 15 divided by 4

var number1 = 5;
var number2 = 10;
alert("The result of the multiplication is: " + (number1 * number2));