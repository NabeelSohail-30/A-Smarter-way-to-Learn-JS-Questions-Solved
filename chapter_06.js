// Short form of x = x + 1 using legal expressions:
x += 1; // or x++

// The fastest way to reduce the value of x from 100 to 99 with a math expression is to use the decrement operator x--

// The value of y after the code var x = 50; var y = x++; will be 50. The x++ operator first assigns the current value of x to y and then increments x. So, y gets the value of x before the increment.

// The value of z after the code var y = 50; var z = --y; will be 49. The --y operator first decrements y and then assigns the decremented value to z.

var newNum = num--;

var variable1 = 10;
var variable2 = variable1++;

var myNumber = 42; // Assign a number value
myNumber++; // Increment the variable
alert("The new value is: " + myNumber); // Display the new value in an alert
