var calculatedNum = 2 + (2 * 6);
// The value of calculatedNum is 14.

var calculatedNum = (2 + 2) * 6;
// The value of calculatedNum is 24.

var calculatedNum = (2 + 2) * (4 + 2);
// The value of calculatedNum is 24.

var calculatedNum = ((2 + 2) * 4) + 2;
// The value of calculatedNum is 18.

// Statement to assign to cost the result of 2 + 2 * 4 + 10, clarified with parentheses:
var cost = (2 + 2) * (4 + 10); // Produces 56

// Statement to assign to units the result of 2 + 2 * 4 + 10, clarified with parentheses:
var units = (2 + 2 * 4) + 10; // Produces 20

// Statement to assign to pressure the result of 4 / 2 * 4, clarified with parentheses:
var pressure = (4 / 2) * 4; // Produces 8